{
  "name": "svagelj_tin_mms",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "watch:sass": "sass --watch src/style:_site/css",
    "build:sass": "sass src/style:_site/css",
    "watch:eleventy": "eleventy --serve",
    "build:eleventy": "eleventy",
    "start": "npm-run-all build:sass --parallel watch:*",
    "build": "npm-run-all build:*"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1",
    "npm-run-all": "^4.1.5",
    "sass": "^1.70.0"
  }
}
